//Didnt work
//
//Time Complexity 0(n^2)
#include "stdafx.h"
#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;
/*
int main(){
//	int Array[] = { 3, 10, 2, 1, 20 };
//	int sizeOfArray = sizeof(Array) / sizeof(Array[0]);
//	constructLisAndPrint(Array, sizeOfArray);


	vector<int> A = { 9, 4, 2, 10, 10, 6, 7, 8, 8, 1, 9 };
		// write your code in C++14 (g++ 6.2.0)
		int height; int times = 1;
		vector<int>::iterator itr;
		int sizeOfVector = A.size();
		cout << "sizeOfVector" << sizeOfVector << endl;
		if (sizeOfVector == 1)
			return 1;
		else{}
		int i = 0;
		int a[10];
		for (itr = A.begin(); itr < A.end() - 1; itr++){
			if (*itr != *(itr + 1)){
				cout << times << " times" << endl;
				times++;
				
			}
			else{
				a[i] = times;
				times = 0;
			}
		
		}
		sort(a, a + 5);
		int max = a[0];
		for (int i = 0; i < 5; i++){
			if (a[i] > max)
				max = a[i];
		}
		cout << "Max" << max-1 << endl;
		return max -1;


}*/