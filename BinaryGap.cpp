
#include "stdafx.h"
#include <iostream>
#include <bitset>
#include <sstream>
using namespace std;



int sample()
{
	/*int rem, start = 0;
	int N = 561892;
	std::cout << N;
	std::string binary = std::bitset<32>(N).to_string();//to binary
	//while (N != 0) { binary = (N % 2 == 0 ? "0" : "1") + binary; N /= 2; }
	//auto u_long = std::bitset<32>(N).to_ullong();//to binary

	cout <<" Binary equivalent is " << binary << "\n";
	//cout << " u_long equivalent is " << u_long << "\n";
	stringstream integer(binary);
	auto x = 0 , count = 0, binGap = 0;
	//long z;
	integer >> x;
	cout << "Value of x : " << x;
	size_t u;
	long u_long = std::stol(binary, &u);
	cout << " u_long equivalent is " << u_long << "\n";
	// Now the variable x holds the value 12345
	
	while (x > 0){

		rem = x % 10;
		if (rem == 1){
			if (count > 0){
				binGap = count;
			}
			count = 0;
			start = 1;
		}
		else
		{
			if (start != 0)
				count++;
		}
			cout << " Binary gap = " << binGap << endl;
		x = x / 10;
	}*/
	//return binGap;
	getchar();
	return 0;
}